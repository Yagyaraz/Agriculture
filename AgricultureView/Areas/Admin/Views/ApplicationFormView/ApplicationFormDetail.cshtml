@* @model ApplicationFormViewModel
@{
    ViewData["Title"] = "ApplicationForm Details";
}

<div class="page-inner">
    <div class="page-header">
        <h3 class="fw-bold mb-3">तालिम विवरण</h3>
        <ul class="breadcrumbs mb-3">
            <li class="nav-home">
                <a asp-controller="Home" asp-action="Index">
                    <i class="icon-home"></i>
                </a>
            </li>
            <li class="separator"><i class="icon-arrow-right"></i></li>
            <li class="nav-item">
                <a asp-controller="ApplicationFormView" asp-action="ApplicationForm">ApplicationForm List</a>
            </li>
            <li class="separator"><i class="icon-arrow-right"></i></li>
            <li class="nav-item"><a href="#">ApplicationForm Details</a></li>
        </ul>
    </div>

    <div class="card">
        <div class="card-header">
            <h4 class="card-title">तालिम विवरण</h4>
        </div>

        <div class="card-body">

            <div class="row">

                <div class="col-md-3 mb-3 d-flex gap-2">
                    <strong>आर्थिक वर्ष :</strong>
                    <div>@Model.FiscalYearId</div>
                </div>

                <div class="col-md-3 mb-3 d-flex gap-2">
                    <strong>शिर्षक :</strong>
                    <div>@Model.Title</div>
                </div>

                <div class="col-md-3 mb-3 d-flex gap-2">
                    <strong>प्रशिक्षक :</strong>
                    <div>@Model.TrainerName</div>
                </div>

                <div class="col-md-3 mb-3 d-flex gap-2">
                    <strong>स्थान :</strong>
                    <div>@Model.ApplicationFormPlace</div>
                </div>

                <div class="col-md-3 mb-3 d-flex gap-2">
                    <strong>प्रिमियम प्रकार :</strong>
                    <div>@(Model.PremiumId == 1 ? "प्रीमियम" : "नि:शुल्क")</div>
                </div>

                @if (Model.PremiumId == 1)
                {
                    <div class="col-md-3 mb-3 d-flex gap-2">
                        <strong>ट्रेनिङ शुल्क :</strong>
                        <div>@Model.Amount</div>
                    </div>
                }

                <div class="col-md-3 mb-3 d-flex gap-2">
                    <strong>ट्रेनिङ प्रकार :</strong>
                    <div>@Model.ApplicationFormTypeName</div>
                </div>

                <div class="col-md-3 mb-3 d-flex gap-2">
                    <strong>सुरु मिति :</strong>
                    <div>@Model.StartDate</div>
                </div>

                <div class="col-md-3 mb-3 d-flex gap-2">
                    <strong>अन्तिम मिति :</strong>
                    <div>@Model.EndDate</div>
                </div>

                <div class="col-md-3 mb-3 d-flex gap-2">
                    <strong>आयोजक :</strong>
                    <div>@Model.Organizer</div>
                </div>

            </div>

            <hr />

            <div class="mb-3">
                <strong>विवरण :</strong>
                <div class="border p-2" style="min-height: 120px;">
                    @Html.Raw(Model.Description)
                </div>
            </div>

            <hr />

            <div class="mb-3">
                <strong>फोटो :</strong>
                <div class="pt-2">
                    @if (!string.IsNullOrEmpty(Model.FilePath))
                    {
                        <img src="@(_global.BaseUrl() + Model.FilePath)"
                             class="img-thumbnail"
                             style="width:220px;" />
                    }
                    else
                    {
                        <div class="text-muted">No photo available</div>
                    }
                </div>
            </div>

        </div>

        <div class="card-action">
            <a asp-action="ApplicationForm" asp-controller="ApplicationFormView" class="btn btn-secondary">Back</a>
        </div>
    </div>
</div>
 *@